PORT=3000
# 바인딩 주소(선택). 미설정 시 127.0.0.1 = 로컬 전용(권장, nginx가 같은 호스트에서 프록시).
# 컨테이너 등 외부 바인딩이 필요할 때만 0.0.0.0
HOST=127.0.0.1
JWT_SECRET=change-me-must-match-gnuboard-extend-secret
DATABASE_URL="mysql://user:pass@localhost:3306/samplepcb_app"
