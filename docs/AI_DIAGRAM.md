# AI 연동 — 구성 명세 생성과 결정적 시스템 구성도 렌더

2026-07-12. 재능마켓 의뢰 위저드에 "시스템 구성도" 스텝을 추가하고, 그 기반으로 **범용 AI
유스케이스 실행 계층**을 신설했다. 프롬프트는 프로빙(모델 5종 × 변형 3종, 13런 + 사용자
피드백)으로 확정한 것이 기본값이다.

같은 날 2차로 **인터뷰 파이프라인(Phase 1)** 을 얹었다 — 질문 기획 PDF(질문 설계서 v3 ·
ROC 질문 뱅크 v4) 기반, 별도 프로빙(P1~P4, 11런, `.tmp/ai-interview-probing/`)으로 검증 후
구현. 상세는 §6.

2026-07-15에는 생성 원천과 저장 산출물의 정합성을 보강했다. STEP 2의 세부분야·요구 툴을
AI 입력에 포함하고, 원천 입력 변경 시 기존 산출물을 자동 제외하며, 명세 없는 파생 문서 저장을
서버가 거부한다. LLM HTML 렌더에는 CSP 전처리를 추가했다.

같은 날 후속으로 `DiagramSpec → HTML/SVG` LLM 호출을 `@sp/utils`의 결정적 렌더러로
교체했다. 인터뷰 경로는 구조화 호출만 기다린 뒤 구성도를 즉시 그리며, 서버가 저장 직전에
같은 렌더러로 다시 생성한다. 기존 LLM HTML 프로젝트와 설명 기반 전자 분야 폴백은 유지한다.

AI 단계를 최종 검토로 옮긴 뒤에는 ROC·분야 카드에 예산·시작/완료 희망일·견적 마감·견적
방식도 전달한다. 모든 유스케이스 앞에 고객 자유 입력을 명령이 아닌 자료로 취급하는 고정
보안 정책을 붙이고, 동일 회원의 동일 모델·프롬프트·입력 성공 결과는 잡 TTL 동안 재사용한다.
프로젝트 등록 시에는 서버가 jobId의 소유자·유스케이스·입력 해시·출력을 다시 대조해 모델·
프롬프트 버전·입력/출력 해시를 저장한다. 이후 원천이나 산출물이 바뀌면 상세 라벨은 자동으로
"고객 수정본"이 되며, 메타데이터가 없는 과거 데이터는 "출처 미확인"으로 표시한다.
인터뷰 원문은 신규 의뢰에서 명시 동의한 경우에만 저장·공개하고, 견적 가능한 전문가와 채택
전문가에게 원문 대조용으로 보여준다. ROC에는 계약 비구속 AI 초안 고지를 서버가 강제로 붙이며,
입찰 채택 트랜잭션은 당시 의뢰 조건·AI 산출물·공개 동의 답변·채택 견적을 계약에 스냅샷한다.
관리자 AI 설정에는 저장 전 모델·프롬프트를 비식별 고정 샘플로 실제 실행하고 산출을 미리 보는
테스트를 추가했다. 일반 의뢰와 공용 러너를 사용해 재시도·파싱·스키마 검증 차이가 없다.

2026-07-16에는 질문 정책을 `talent_market_ai_requirement_question_policy.md` 기준으로
교체하고, 등록 전 첨부의 텍스트·이미지를 직접 분석하는 multipart 구조화 경로를 추가했다.
질문은 한 화면 5개·전체 15개 이하이며, 첨부 분석은 별도 고지 후에만 AI 호출에 사용한다.
사용자가 선택하면 설명·첨부를 먼저 보수적으로 판독해 이미 명확히 답한 후보만 제외하는
비동기 선분석도 같은 날 추가했다. 같은 날 밤 의뢰 위저드를 AI-우선 4스텝(분야→설명·자료→
AI 인터뷰→검토·등록)으로 전면 개편하고, 선분석 프롬프트를 v2로 올려 "제가 이해한 내용"
(`understood`) 요약을 함께 반환하게 했다.

## 1. 구조 (3계층)

| 계층 | 저장/코드 | 내용 |
|---|---|---|
| 연결 | `sp_config` (`ai_base_url`, `ai_api_key`) | Ollama 주소·키. 키 원문은 어떤 API 응답에도 없음(마스킹만) |
| 유스케이스 | `sp_ai_usecase` (useCase PK) | enabled·model·promptTemplate. 행은 레지스트리 기준 **lazy 생성** |
| 실행 | `apps/api/src/lib/ai/` + `routes/ai.ts` | Ollama 클라이언트 + 유스케이스 레지스트리 + 공용 러너 + 인메모리 잡 |
| 출처 | `sp_market_project.aiGenerationMeta` | 검증한 jobId·모델·프롬프트/입력/출력 해시와 결정적 렌더 버전 |

- **라우트는 범용, 정책은 케이스별**: `POST /api/ai/:useCase/run` 은 공통이지만 입력 스키마·
  프롬프트 바인딩은 `lib/ai/usecases.ts` 레지스트리가 유스케이스별로 명시한다.
- **비동기 잡**: 구조화·ROC·포스팅과 레거시 구성도 생성은 run 이 jobId 즉시 반환 →
  `GET /api/ai/jobs/:id` 5초 폴링. 잡은 인메모리(단일 인스턴스 전제) — 서버 재시작 시
  소실=클라이언트 재시도. 명세 기반 구성도 렌더는 로컬 동기 처리라 잡을 만들지 않는다.
- **동일 입력 캐시**: 회원·유스케이스·모델·프롬프트 해시·입력 해시가 모두 같고 완료된 성공
  잡이 TTL 안에 있으면 새 LLM 호출 없이 기존 jobId를 반환한다. 사용자 사이 결과는 공유하지 않는다.
- **영속 provenance**: 등록 payload의 jobId는 힌트일 뿐 신뢰하지 않는다. 서버가 완료 상태·
  소유 회원·유스케이스·현재 입력 해시·저장 산출물 일치를 모두 확인한 항목만 `ai-generated`로
  기록한다. 명세 기반 SVG는 렌더러 버전과 명세/출력 해시를 `deterministic`으로 기록한다.
- **스트리밍 필수**: Ollama 호출은 `stream:true` 로 받는다. 비스트림은 undici 헤더 타임아웃
  (~300s)에 걸려 장시간 생성이 실패한다(프로빙 실측).

## 2. 유스케이스: market.request-diagram

- 위저드 v2(분야→설명·자료→AI 인터뷰→검토·등록)에서 이 레거시 단발 유스케이스의 위저드
  UI는 제거됐다 — 신규 의뢰 구성도는 인터뷰→structurize→결정적 렌더 단일 경로다. 유스케이스·
  직접 API·관리자 설정·기존 프로젝트 표시는 존치한다. AI 산출물은 모두 선택 사항이며,
  생성하지 않거나 미완료 상태로 제출하면 해당 산출물 없이 등록된다.
- 결과는 `sp_market_project.diagramHtml`(MEDIUMTEXT, 512KB 상한)에 저장. 공개 범위는
  description 과 동일(설명에서 파생된 것). 상세 화면(고객·관리자) 표시.
- **렌더는 반드시 sandbox iframe(srcdoc)** — LLM 산출 HTML 을 DOM 에 직결하면 XSS.
  `diagram-srcdoc.ts`가 script·embed·form 등 활성 요소와 기존 보안 메타를 제거하고, 외부 연결을
  차단하는 CSP(`default-src 'none'`, 이미지는 data URL만)를 주입한다.
- **외부 전송 고지는 설명 스텝의 "AI 분석 동의"(기본 on)로 통합** — 동의가 선분석·구조화·
  첨부 전송을 함께 게이트한다(해제 시 AI 단계 전체 제외). 일반 구조화는 제목·분야·설명·인터뷰
  답변, ROC·분야 카드는 여기에 예산·견적 마감·견적 방식을 추가한다(희망 시작/완료일 입력은
  v2에서 제거 — 계약 필드는 null 전송). 첨부가 있는 구조화는 서버가 제한 추출한 문서 텍스트와
  래스터 미리보기도 전송한다.
- 재생성 횟수 제한은 현재 없음(사용자 결정) — 남용 시 잡 시작 지점에 rate limit 추가.

## 3. 프로빙 확정 사항 (2026-07-12)

- **기본 모델 `glm-5.2:cloud`**(1위, ~170초) · 차선 `deepseek-v4-pro:cloud`(71초) ·
  `minimax-m3` 는 내용 최다지만 720초라 부적합 · `gpt-oss:120b` 는 레이아웃 붕괴.
- **레거시 HTML 구성도 생성에는 참조 이미지 불필요**: 이미지 첨부 변형은 텍스트 상세 명세
  변형보다 레이아웃이 무너졌다.
  참조 이미지의 내용 커버리지를 "도메인 중립 체크리스트"(전원 체인·저장/디버그·외부 시스템
  패널·옵션류)로 텍스트화한 프롬프트가 전 모델에서 승리 → **멀티모달 제약 없음**.
- 최소 지시는 스타일 미달 — 레이아웃 골격(3열·단일 svg viewBox·직교 연결선)과 색·표기
  규칙을 명시하는 것이 결정적.
- 부품은 역할명 + (TBD)/(Option)만 — 구체 모델명 표기 금지(사용자 확정).
- 기본 프롬프트 정본: `lib/ai/usecases.ts` `DIAGRAM_DEFAULT_PROMPT`(관리자가 화면에서 수정
  가능 — DB 값이 우선, 코드 기본값은 신규 행 생성 시에만 쓰임).

## 4. 관리자 화면

`/app/admin/settings` → "AI 연동" 탭:
- 연결: API 주소(기본 `http://127.0.0.1:11434` — 로컬 데몬이 클라우드 모델 프록시, 키 불요) ·
  API 키(입력=교체·비움=유지·체크=삭제, 저장 후엔 마스킹만 표시) · **연결 테스트**(모델 목록
  조회 — 성공 시 모델 셀렉트 datalist 로 사용).
- 유스케이스: 사용 토글 · 모델 · 프롬프트 템플릿. 각 카드의 **샘플 테스트 실행**은 저장하지
  않은 현재 모델·프롬프트를 코드에 고정한 비식별 샘플로 실행한다. 활성 토글·DB 설정은 바꾸지
  않고 동일 입력 캐시도 우회하므로 실제 연결·모델을 검증한다.
- 테스트 결과는 같은 비동기 잡 API로 폴링한다. 구조화 JSON은 결정적 SVG로 렌더하고 레거시
  HTML은 sandbox+CSP iframe으로 격리하며, JSON·마크다운·HTML 원문도 확인할 수 있다.
  구성 명세 렌더 자체는 AI 유스케이스가 아니므로 관리자 모델·프롬프트 목록에 나타나지 않는다.

## 5. 운영 메모

- **연결 우선순위: `.env`(AI_BASE_URL·AI_API_KEY) > 관리자 화면 저장값 > 기본값**
  (`http://127.0.0.1:11434`). 운영 키는 `.env` 파일 관리 권장(DB 에 안 남음) — env 가
  잡혀 있으면 관리자 화면의 해당 입력은 잠기고 ".env 값 우선" 안내가 뜬다.
  `.env.example` 의 AI 섹션 참조. env 변경은 API 재시작 후 반영.
- 운영 서버에 로컬 Ollama 가 없으면 `AI_BASE_URL=https://ollama.com` + API 키(ollama.com
  발급)로 전환한다 — 클라이언트는 동일 코드 경로(Authorization Bearer). ollama.com 직결
  시 모델명에 `:cloud` 접미사가 없을 수 있으니 연결 테스트 목록에서 재선택.
- 첨부에 래스터/PDF 미리보기가 있으면 `AI_ATTACHMENT_VISION_MODEL`을 사용하며 기본값은
  `qwen3.5:cloud`다. 텍스트만 추출된 첨부는 관리자 `market.request-structurize` 모델을 쓴다.
- 마이그레이션 `20260712200000_ai_usecase_diagram`(sp_ai_usecase CREATE + diagramHtml ADD) —
  additive, `migrate deploy` 전용.
- 새 유스케이스 추가 = 계약 `AI_USECASES` + 레지스트리 def + (필요시 FE) — 설정 행·화면은
  자동(lazy 생성·목록 렌더).
- 과거 `market.request-diagram-spec` 설정 행은 DB에 남아도 계약 허용목록에서 제외되어
  실행·관리 화면에 노출되지 않는다. additive 원칙상 삭제 마이그레이션은 만들지 않는다.
- provenance 마이그레이션 `20260715190000_market_ai_provenance`는 JSON 컬럼 하나를 추가하는
  additive 변경이다. 기존 프로젝트는 소급해서 AI 생성으로 추정하지 않고 출처 미확인으로 둔다.
- E2E: `e2e-market.mts` 에 diagramHtml 왕복 + diagramSpec 왕복·파손 400 + rocMd 왕복·
  인터뷰 답변 공개 동의/권한 + 포스팅 카드 왕복·분야 필터 + 전체서비스 입찰 403을 유지한다.
  LLM 실호출은 E2E 에 없음(Ollama 의존) — 실생성 검증은 관리자 AI 설정의 샘플 테스트로 한다.

## 6. 인터뷰 파이프라인 (Phase 1, 2026-07-12)

질문 기획 PDF(질문 설계서 v3 · ROC 질문 뱅크 v4)의 파이프라인
`질문/답변 → 구조화 → 구성도(+후속 문서)` 중 Phase 1(인터뷰+명세+구성도)을 구현.
**사용자 확정 3건**: 코어 10문항 내외 · ROC(작업검토지시서)는 Phase 2로 분리 ·
갭 감지(P3)는 별도 호출 없이 P1 의 questions_missing 으로 흡수.

### 구조 — spec JSON 이 피벗

- AI 유스케이스 `market.request-structurize`가 답변을 **DiagramSpec JSON**으로 만들고,
  `@sp/utils` `renderDiagramSpecHtml`이 spec→구성도 HTML/SVG를 결정적으로 변환한다. 서버도
  spec과 구성도가 함께 저장될 때 클라이언트 HTML을 신뢰하지 않고 같은 함수로 재생성한다.
  기존 `market.request-diagram`
  (설명→HTML 단발)은 **인터뷰 비활성 시 전자 분야(circuit/pcb/firmware) 전용 폴백**으로
  유지 — 프롬프트가 DB(관리자 소유)인 하드웨어 전용 계약이므로 순수 SW 의뢰에는 노출하지
  않고, 직접 API 호출도 409 `USECASE_NOT_APPLICABLE`로 거부한다.
- 위저드 게이트(v2): structurize 활성 && 설명 스텝의 AI 분석 동의(기본 on)일 때만 "AI
  인터뷰" 스텝이 존재하고 검토 스텝에 AI 명세 섹션이 나타난다. 동의 해제·비활성 시 스텝
  배열은 [분야, 설명·자료, 검토·등록]으로 줄고 일반 등록만 가능하다. legacy diagram 단발
  UI는 v2에서 제거(유스케이스는 존치).
- **질문 뱅크는 데이터(코드)**: `@sp/api-contract` `AI_INTERVIEW_QUESTIONS`는 정책 원문
  77문항(공통 10·시스템 연결 7·회로 8·PCB 7·펌웨어 9·기구 8·제품디자인 8·앱 10·서버 10)과
  정책에 없는 Linux/Windows용 일반 소프트웨어 보완 3문항, 총 80문항이다. 제목·설명과 개발
  분야는 앞 단계 입력을 재사용해 `COMMON-01/07`을 다시 묻지 않는다. `selectAiInterviewQuestions`
  가 FE 노출과 서버 미응답 계산의 단일 판정 함수이며 한 번에 5개, 전체 15개 이하로 선택한다.
  시스템 통합은 별도 서비스 분야가 아니라 `requestType=system`에 매핑해 공통 최대 8 + 연결 4 +
  선택 분야 3으로 구성하고, `etc`는 공통만 사용한다. 전 문항은 선택 사항이고 모든 선택형에는
  `잘 모르겠습니다` 또는 `전문가 추천` 탈출구가 있다.
- 최초 후보는 분야·의뢰유형·앞 단계 필드에 따른 결정적 우선순위로 최대 15개를 먼저 고른다.
  사용자가 **"AI가 설명·첨부를 먼저 확인해 질문 줄이기"**를 선택하면 별도 비동기 선분석이
  후보별 명시 근거를 확인하고, 완전히 답한 코드만 후보에서 제거한다. 제거 뒤 낮은 우선순위
  질문으로 다시 채우지 않는다. 선분석을 건너뛰거나 실패해도 결정적 후보로 계속 진행할 수 있다.
- 관리자 DB에 과거 하드웨어 전용 프롬프트가 저장돼 있어도 실행 시 `분야 적용 정책`을
  앞에 붙인다. 순수 앱·서버·Linux/Windows SW에는 전원·MCU·센서·PCB 블록 생성을 금지하고,
  제품·기구 단독 의뢰도 전자 하드웨어가 명시된 경우에만 해당 블록을 허용한다.
- STEP 2의 `categories`·`cadTools`는 코드=한글 라벨의 고정 기술 컨텍스트로 프롬프트 앞에
  붙인다. 관리자 DB에 저장된 과거 템플릿을 수정하지 않아도 구조화·ROC·포스팅이 요구 기술을
  함께 판단한다. 고객 입력 안의 역할 변경·이전 지시 무시·검수 통과 강제 문구는 명령이 아닌
  요구 자료로만 취급한다는 고정 보안 정책도 관리자 템플릿보다 앞에 붙인다.
- 위저드 UX(v2): 분야 → 설명·자료(AI 분석 동의) → AI 인터뷰 스텝 진입 시 선분석 자동 실행
  ("제가 이해한 내용" 카드 + 제외 질문 근거 접이식 + 축소된 질문 5문항씩, 전체 최대 15 —
  전부 선택, 결과 보기 CTA는 답변 유무에 따라 "질문 건너뛰고 결과 보기"/"답변 반영해 결과
  보기"로 전환) → 검토 스텝 진입 시 구조화 자동 시작(~30초~3분, 대기 중 조건 입력은 가능하나
  포함 예정 산출물이 생성 중이면 등록 차단 — 사유 안내 + 생성 중인 것만 빼는 건너뛰기 버튼,
  선분석 진행 중이면 완료를 기다렸다 축소된 질문 집합으로 실행) →
  **요약 카드(블록·그룹·TBD) + AI 추가 질문(questions_missing 보강 → 재구조화)** →
  "이 명세로 구성도 만들기"(즉시) → ROC·분야 카드는 접이식 "추가 AI 문서(선택)".
  명세와 구성도는 별도 포함 체크박스로 선택한다. 신선도 서명은 2계층 — 제목·설명·의뢰 유형·
  분야·답변·질문 코드·첨부 메타가 바뀌면 명세·구성도·파생 문서 전체가, 예산·견적 마감·견적
  방식만 바뀌면 ROC·분야 카드만 오래된 상태로 표시되고 제출에서 제외된다.
- 저장: `sp_market_project.diagramSpec`(MEDIUMTEXT, 정규화 직렬화본) — 구성도의 원천
  데이터이자 Phase 2(ROC·포스팅 요약) 파생의 근원. 공개 범위는 description 동일.
  ROC·포스팅은 명세가 있어야 저장할 수 있으며 create/PATCH 양쪽에서 최종 상태 불변식을
  검증한다. 저장 시 AI 잡 원천이 증명된 항목만 생성 메타데이터를 남기고, 이후 프로젝트 원천
  또는 산출물 PATCH는 메타데이터를 무효화해 고객 수정본으로 표시한다.
  마이그레이션 `20260712230000_market_diagram_spec`(additive).

### 최초 질문 선분석 (2026-07-16)

- `POST /api/ai/market.request-structurize/preanalyze-questions`는 항상 multipart로 요청하고
  `payload`와 등록 전 첨부를 받는다. 기존 `market.request-structurize`의 활성 여부·텍스트 모델을
  재사용하며, 이미지가 있으면 `AI_ATTACHMENT_VISION_MODEL`을 사용한다. 공용 비동기 잡·폴링·
  동일 입력 캐시를 사용하되 `mode=question-preanalysis-v2`와 별도 프롬프트 버전으로 최종
  구조화 잡과 구분한다.
- **v2(위저드 개편과 동시)**: 프롬프트가 후보 판정과 함께 자료에 명시된 내용만 담는 "제가
  이해한 내용"(`understood` — product·problem·users·environment·coreFunctions≤5·
  materials=보유 자료·진행 단계) 요약을 반환한다. 계약 필드는 optional이라 구형 응답·캐시
  잡도 통과하며, 근거 없는 필드는 통째로 생략한다(빈 문자열 금지). FE는 인터뷰 스텝 상단
  카드로 표시하고 등록 payload에는 넣지 않는다.
- 모델은 전달받은 후보 코드만 판정하며, 제목·설명·앞 단계 필드·첨부에 **명시적으로 있고 질문의
  핵심을 완전히 답한 경우**에만 `knownQuestionCodes`와 300자 이하 `evidence`를 함께 반환한다.
  추론·관행·부분 답변은 제외 근거가 아니다. 서버 파서는 후보 밖 코드, 근거 없는 코드, 중복을
  제거하고 결과 계약을 통과하지 못한 응답은 한 번 재시도한다.
- 제목·분야·세부분야·도구·설명·첨부 메타가 바뀌면 진행 중/완료 선분석을 폐기하고 최초 후보로
  복귀한다. 선분석 전에 사용자가 직접 쓴 답변은 해당 질문이 숨겨져도 최종 구조화 입력에 보존한다.
  등록 payload의 일회성 `aiQuestionCodes`는 최종 구조화 잡 입력 해시 재검증에만 쓰고 DB에는
  저장하지 않는다.
- `glm-5.2:cloud` 실제 프로브(상세 저온 창고 요구사항 Markdown 첨부)는 28초에 후보 14개 중
  명시 근거가 충분한 11개를 제외하고 3개를 남겼다. 현재 단계 미기재, 정전 시 동작까지는 답하지
  않은 장애 정책, Ethernet 명시만으로 고속 신호 요구를 확정할 수 없는 항목을 유지해 부분 답변을
  과잉 제거하지 않았다.

### 검증 계층 (프로빙 실측 기반)

- **DiagramSpec zod 스키마**: enum 이탈은 `.catch`로 안전값 흡수(프로빙 실측 — glm·deepseek
  모두 SWD 연결에 `flow:"debug"` 슬립), 구조 결함은 `normalizeDiagramSpec`이 보정(미정의
  그룹 자동 생성·끊긴 연결 제거·중복 블록 제거). **실패 대신 복구가 원칙.**
- 러너: parseResult throw 시 동일 프롬프트 재호출(structurize 는 1회) — JSON 완전 파손만
  재시도 대상. 서버 저장 시에도 재검증(`parseDiagramSpecString`) — 파손 spec 은 400
  `INVALID_DIAGRAM_SPEC` (이관 specJson `_legacy` 교훈: 저장 전 형태 통제).
- 프롬프트 보강: 그룹 수 2~7 상한(8개→렌더 겹침 실측) · 첨부의 수치·단위·수량·일정·
  포함/제외 조건 전량 보존 · 자료에 없는 연결 인터페이스는 `(TBD)`로 강등 ·
  하드웨어/소프트웨어별 그룹
  후보 분리(순수 SW 의뢰에 전원·MCU 블록 생성 금지) · 서버 연동
  답변 시 External System 블록 강제 · 미확정 점검 체크리스트(안테나·인증·방열·검사·커넥터
  ·소비전류) · 답변에 없는 모델명 금지.
- 결정적 렌더러: 그룹을 3열에 배치하고 블록 타입별 색·상태(TBD/Option)·직교 연결·제약·
  하이라이트·범례를 단일 SVG로 만든다. 문자열은 XML 이스케이프하며 외부 리소스·스크립트를
  생성하지 않는다. 소프트웨어 명세는 client·service·api·database·cache·queue·worker·
  operations 타입을 보존한다. 계약 최대 80블록·160연결에서도 512KB 저장 상한 이내임을 테스트한다.

### 첨부 분석 경로 (2026-07-16)

- `POST /api/ai/market.request-structurize/run-with-attachments`가 기존 질문 payload와 등록 전
  첨부를 multipart로 받는다. 원본을 AI 잡에 저장하지 않고 SHA-256만 캐시·provenance 원천에
  남긴다. 프로젝트 등록 시 실제 첨부 해시와 잡 원천을 다시 대조한다.
- 텍스트/소스/CSV/JSON/Gerber/KiCad는 UTF-8, DOCX/PPTX는 문서 XML과 포함 래스터,
  XLSX는 최대 12시트·시트당 500행, PDF는 최대 50쪽 텍스트와 앞 3쪽 미리보기를 추출한다.
  직접 이미지와 합쳐 비전 입력은 최대 4개다. 미지원 STEP 등 바이너리는 파일 메타데이터만
  알리고 내용을 추정하지 않는다.
- 분석 상한은 파일 10개·합계 50MB·파일당 텍스트 20,000자·전체 텍스트 80,000자·이미지당
  8MB다. Office ZIP은 필요한 엔트리만 풀고 엔트리 16MB·압축 해제 합계 32MB를 넘으면 차단한다.
- 첨부에 답이 있는 미응답 항목은 `questions_missing`에서 반복하지 않게 지시한다. 결과 단계에서는
  실제 제목·설명·답변·첨부 근거에 없는 UART/GPIO/SPI/전압 등의 연결 인터페이스를 `(TBD)`로
  결정적으로 낮춘다.

### 첨부 프로빙 근거 (2026-07-16)

- 2쪽짜리 저온 창고 모니터 요구 PDF를 직접 생성·렌더 확인한 뒤 같은 제목/설명으로
  `glm-5.2:cloud` 텍스트 기준안과 `qwen3.5:cloud` 첨부안을 비교했다. PDF 텍스트 885자와
  미리보기 2장이 경고 없이 추출됐다.
- 골든 사실 10개(12V, -25~5°C, IP65, ±0.3°C, Ethernet, MQTT, 24시간 저장,
  80×50mm, 시제품 10대, KC)는 기준안 1/10, 첨부안 10/10이었다. 첨부안은 이미 확정된 전원·
  통신·치수·인증을 다시 묻지 않고, 센서 인터페이스·저장 매체·커넥터·서버 인증 등 실제
  미확정 항목만 질문했다.
- 반복 프로빙에서 그룹 8개, 내부 3.3V/5V 추정, `LED 3개→3색 LED`, 일정/수량 누락을 발견해
  위 불변 정책과 결과 보정을 추가했다. 최종 결과는 그룹 6~7개, 골든 사실 10/10,
  미확정 내부 인터페이스 `(TBD)`를 만족했다.
- 기존 기본 후보 `qwen3-vl:235b-cloud`는 실호출에서 2026-06-16 retired(HTTP 410)였으므로
  현재 이미지 입력을 지원하는 `qwen3.5:cloud`로 교체했다.

### 프로빙 근거 (2026-07-12, `.tmp/ai-interview-probing/`)

- P1 구조화 4런: JSON 유효 4/4·환각 0·TBD 규율 준수. P2 렌더 2런: 라벨 충실도 49/49
  (골든 30/30 + P1 산출 파이프라인 19/19). P3 갭 감지 3런: 크리티컬 재현율 16/16,
  부분 답변 인지 후속질문. P4 ROC 1런: 서식 10/10 — Phase 2 근거 확보.
- 당시 임시 `.tmp/smoke-interview.mts`로 활성화→structurize→렌더→원복을 실측했으나 해당
  파일은 보존하지 않았다. 현재 자동 테스트는 관리자 비식별 샘플이 모든 입력 계약·프롬프트
  바인딩을 만족하는지도 검증하고, 실제 모델 호출·산출 미리보기는 관리자 화면에서 수행한다.

### Phase 2 — 작업검토지시서 (2026-07-12 구현)

- 유스케이스 `market.request-roc`: 구성 명세 + 인터뷰 답변 + 의뢰 텍스트 + 예산·일정·견적
  조건 → 10섹션
  마크다운 지시서(프로빙 P4 프롬프트의 의뢰 분야 일반형). 서식 게이트(섹션 8개 미만
  재시도 1회) + (TBD)·9번 수집 규율.
- **노출 정책**: ROC는 description 과 동일(구성도와 같은 원칙)이며 화면과 문서 본문에
  "AI 생성 초안·계약 일부 아님·검수 기준은 계약 시 별도 확정"을 고정 표시한다.
  인터뷰 원문은 신규 등록에서 공개 동의한 건만 소유자·관리자·현재 견적 가능 전문가·채택
  전문가에게 노출한다. 기존 `interviewAnswersSharedAt=null` 건은 소급 공개하지 않는다.
- 저장: `sp_market_project.rocMd`(MEDIUMTEXT) + `interviewAnswers`(JSON) + 공개 동의 시각
  `interviewAnswersSharedAt`. 원문은 재생성·Phase 3 파생의 근원이면서 AI 문서 대조 근거다.
  최초 컬럼 마이그레이션은 `20260713010000_market_roc_answers`, 공개 동의 시각은
  `20260715191000_market_ai_disclosure_snapshot`.
  원천(spec) 제거 시 파생(rocMd) 동반 제거.
- 위저드: 명세 확정 후 "작업검토지시서 생성(선택)"(~1분) → 미리보기 + 첨부 체크.
  게이트는 인터뷰 활성 && roc 활성. 상세 화면(고객·전문가)은 `RocViewer`
  (마크다운 **라인 파서** 렌더 — LLM 산출이므로 v-html 금지).
- 계약 채택 시 `sp_market_contract.requestSnapshot`에 당시 의뢰 조건·AI 산출물·공개 동의된
  답변·provenance 원문·채택 견적을 저장한다. 이는 분쟁 감사용 시점 고정본이며 ROC 자체를
  계약 조항으로 승격시키지 않는다.

### Phase 3 — 분야별 포스팅 카드 + 전체서비스 입찰 제한 (2026-07-12 구현)

**사용자 확정 3건**: ① 단일 의뢰 유지 + 분야별 AI 카드(분리 입찰 아님 — 계약·정산이
프로젝트당 1건 구조) ② 전체서비스(시스템 통합) 제한은 **입찰만**(목록·상세는 공개,
기획 PDF §13.4 의 완화형) ③ 검수자 포스팅은 제외(검수자 역할 자체가 없어 별도 기획).

- 유스케이스 `market.request-postings`: 명세+답변+예산·일정·견적 조건 → 분야별 카드 JSON(요약·작업범위·
  산출물·확인 필요 리스크). 의뢰 분야 코드만 허용, 중복 카드 dedupe, 재시도 1.
- 저장 `sp_market_project.postings`(JSON, `MarketPostingCards`) — 서버가 의뢰 분야 밖
  카드를 걸러 저장, 응답은 `toPostings` 로 형태 정규화(파손분 null). 공개 범위는
  description 동일. spec 제거 시 동반 제거. 마이그레이션 `20260713020000_market_postings`.
- **입찰 정책**: `requestType=system` && `expertType=individual` → 403
  `FULL_SERVICE_COMPANY_ONLY`(market-bids 가드 사슬). FE 는 useExpertMe 로 같은 규칙을
  선반영(버튼 숨김 + 안내 문구), 상세 포스팅 섹션에 제한 배지 표시.
- 위저드: 명세 확정 후 "분야별 카드 생성(선택)" → 카드 미리보기 + 첨부 체크.

검수자 포스팅·분리 입찰(포스팅 엔티티)은 미구현 — 검수자 역할 기획이 서면 별도 트랙.
